{
  "name": "@ag/rules",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Business rules ported from the live Apps Script system: pricing, time, status, driver identity, money, recurrence. Pure and dependency-free so the phone and the server share one implementation.",
  "exports": {
    ".": { "types": "./dist/src/rules/index.d.ts", "default": "./dist/src/rules/index.js" },
    "./types": { "types": "./dist/src/types/index.d.ts", "default": "./dist/src/types/index.js" }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "parity": "tsc && node tools/parity.mjs",
    "verify": "bun run typecheck && bun run test && bun run parity"
  }
}
